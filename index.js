/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import UpdateTodo from './src/screens/UpdateTodo';

AppRegistry.registerComponent(appName, () => UpdateTodo);
    